{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "g++ Build",
      // If you don't set the "DCMAKE_BUILD_TYPE" the symbols will not be found by gdb.
      "command": "cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && make",
      "detail": "Build the project using CMake"
    }
  ],
}